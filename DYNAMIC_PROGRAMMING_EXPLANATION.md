# 动态规划解法详解 - 最长回文子串

## 核心思想

动态规划的核心思想是：**将大问题分解为小问题，通过解决小问题来解决大问题，并记住已经解决的子问题的答案，避免重复计算。**

## 状态定义

对于最长回文子串问题，我们定义：
- `dp[i][j]` 表示字符串 s 中从索引 i 到 j 的子串是否为回文串
- 如果 `dp[i][j] == true`，说明 `s[i..j]` 是一个回文串

## 状态转移方程

```
dp[i][j] = (s[i] == s[j]) && (j - i <= 2 || dp[i+1][j-1])
```

这个方程的意思是：
- 首先，子串的首尾字符必须相等：`s[i] == s[j]`
- 如果子串长度 ≤ 3，我们可以直接判断为回文（因为长度为2时只需要比较两个字符，长度为3时比较首尾字符）
- 如果子串长度 > 3，那么内部子串 `s[i+1..j-1]` 也必须是回文

## 为什么这样是对的？

举个例子，判断 "abba" 是否为回文：

1. 首先检查首尾：s[0]='a' == s[3]='a' ✓
2. 内部子串 "bb" 是否为回文？
3. "bb" 的首尾都是 'b'，且长度=2，所以是回文 ✓
4. 因此 "abba" 是回文 ✓

这就是动态规划的**最优子结构**性质：大问题的解依赖于小问题的解。

## 初始化

- 单个字符一定是回文：`dp[i][i] = true`
- 我们不需要初始化其他值，因为动态规划会按长度顺序填充

## 填充顺序

我们按子串长度从小到大进行填充：

```
长度 1: dp[0][0], dp[1][1], dp[2][2], dp[3][3]
长度 2: dp[0][1], dp[1][2], dp[2][3]
长度 3: dp[0][2], dp[1][3]
长度 4: dp[0][3]
```

## 举例说明

以字符串 "abba" 为例：

### 初始化（长度1）
```
dp[0][0] = true  // "a"
dp[1][1] = true  // "b"
dp[2][2] = true  // "b"
dp[3][3] = true  // "a"
```

### 长度2
- 检查 "ab" (i=0,j=1): s[0]='a' != s[1]='b' → dp[0][1] = false
- 检查 "bb" (i=1,j=2): s[1]='b' == s[2]='b' → dp[1][2] = true
- 检查 "ba" (i=2,j=3): s[2]='b' != s[3]='a' → dp[2][3] = false

### 长度3
- 检查 "abb" (i=0,j=2): s[0]='a' != s[2]='b' → dp[0][2] = false
- 检查 "bba" (i=1,j=3): s[1]='b' != s[3]='a' → dp[1][3] = false

### 长度4
- 检查 "abba" (i=0,j=3):
  - s[0]='a' == s[3]='a' ✓
  - 长度=4 > 3，所以需要检查 dp[1][2]
  - dp[1][2] = true ✓
  - 因此 dp[0][3] = true ✓

## 关键点总结

1. **状态定义清晰**：dp[i][j] 表示 s[i..j] 是否为回文
2. **状态转移简单**：只需要检查首尾字符和内部子串
3. **填充顺序正确**：从小长度到大长度，确保依赖的子问题已解决
4. **避免重复计算**：每个子串只判断一次

## 时间和空间复杂度

- 时间复杂度：O(n²) - 需要填充 n×n 的DP表
- 空间复杂度：O(n²) - 需要 n×n 的二维数组

虽然时间复杂度是 O(n²)，但比暴力解法的 O(n³) 要好很多，而且代码更容易理解。

## 对比中心扩展法

动态规划和中心扩展法都是 O(n²) 时间复杂度，但：
- **动态规划**：空间 O(n²)，需要额外存储所有子串的状态
- **中心扩展法**：空间 O(1)，只需要几个变量

在实际应用中，**中心扩展法更常用**，因为它更节省空间且同样高效。

